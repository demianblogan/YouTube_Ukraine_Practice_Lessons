class Program
{
    static void Main()
    {
        char[] array = { 'a', 'b', 'c', 'd', 'e' };

        for (int i = 0; i < array.Length; i++)
            System.Console.WriteLine(array[i]);
    }
}