class Program
{
    static void Main()
    {
    }

    /// <summary>
    /// Calculates and returns the area of the rectangle
    /// </summary>
    /// <param name="side1">Length of the rectangle's one side</param>
    /// <param name="side2">Length of the rectangle's other side</param>
    /// <returns>Area of the rectangle</returns>
    static int RectangleArea(int side1, int side2) => side1 * side2;

    /// <summary>
    /// Tries to find substring in string.
    /// </summary>
    /// <param name="str">Main string that may contain substring</param>
    /// <param name="subStr">Substring that may be contained in string</param>
    /// <returns>Result of searching</returns>
    static bool HasSubstring(string str, string subStr) => true;

    /// <summary>
    /// Sorts the array by using selection sort.
    /// </summary>
    /// <param name="arr">Array that need to be sorted</param>
    /// <param name="isAscendingOrder">order of sort</param>
    static void Sort(int[] arr, bool isAscendingOrder = true)
    {
        // ... some code ...
    }

    /// <summary>
    /// Reverses all digits of the number.
    /// </summary>
    /// <param name="number">The number which digits must be reversed</param>
    /// <returns>Reversed number</returns>
    static uint Reverse(uint number) => 0;

    /// <summary>
    /// Gives user an opportunity to enter values of elements of the array.
    /// </summary>
    /// <param name="arr">The array which elements must be entered</param>
    static void EnterElements(int[] arr)
    {
        // ... some code ...
    }
}