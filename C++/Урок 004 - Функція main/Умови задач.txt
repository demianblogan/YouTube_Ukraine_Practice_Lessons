1. Прочитати статтю про об'єктний файл:
https://ru.wikipedia.org/wiki/Объектный_модуль

2. Прочитати статтю про функцію:
https://ru.wikipedia.org/wiki/Функция_(программирование)

3. Прочитати статтю про точку входу:
https://ru.wikipedia.org/wiki/Точка_входа

4. Створіть проект "MyFirstProgram". Створіть в ньому файл main. Запустіть програму і ознайомтеся
   з помилками компілятора

5. Напишіть у файлі main наступний код:

	int main()
	{
		return 0;
	}

   І запустіть його.

6. Напишіть у файлі main наступний код:

	#include <iostream>
	int main()
	{
		std::cout << "Damn, what's going on?!" << std::endl;
		return 0;
	}

   Запустіть його і спробуйте самостійно догадатися, що робить ця програма.
   В наступних уроках я розповім вам про всі невідомі вам речі.

7. Зайдіть в налаштування консолі і підлаштуйте її під себе 
   (шрифт, розмір шрифта, колір шрифта, колір заднього фону).

8. Перепешіть код із задачі 5, от тільки без рядка "return 0". Чи запускається
   програма? Чому консоль пише, що повертається значення 0, якщо ви забрали код?
   (Я давав відповідь на це питання в уроці, згадуйте).

9. Напишіть ось такий код:

   int main      (      ){      return    0  ;}

   І вирівняйте це код завдяки комбінації клавіш, про яку я говорив в кінці уроку.